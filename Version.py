import torch
print(torch.__version__) #shows ur torch version, grant that it is as the required (torch==2.4.0+cu121) as there is torch cpu that will push ur cpu and not GPU
print("Always be nice and don't care about being short")
